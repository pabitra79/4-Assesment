<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3a0ca3;
            --accent-color: #4cc9f0;
            --light-bg: #f8f9fa;
            --dark-text: #2b2d42;
            --light-text: #6c757d;
            --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            --hover-shadow: 0 15px 35px rgba(67, 97, 238, 0.12);
        }
        
        * {
            font-family: 'Inter', sans-serif;
        }
        
        body {
            background-color: #fafafa;
            color: var(--dark-text);
            padding-top: 80px;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', sans-serif;
            font-weight: 600;
        }
        
        .page-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 3rem 0;
            margin-bottom: 3rem;
            border-radius: 0 0 20px 20px;
            box-shadow: 0 4px 20px rgba(67, 97, 238, 0.15);
        }
        
        .page-header h1 {
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .page-header .lead {
            opacity: 0.9;
            font-weight: 300;
        }
        
        .product-card {
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            border: none;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--card-shadow);
            height: 100%;
            background: white;
        }
        
        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--hover-shadow);
        }
        
        .product-img {
            height: 220px;
            object-fit: cover;
            width: 100%;
            transition: transform 0.5s ease;
        }
        
        .product-card:hover .product-img {
            transform: scale(1.05);
        }
        
        .product-img-container {
            overflow: hidden;
            height: 220px;
        }
        
        .product-card .card-body {
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
        }
        
        .product-card .card-title {
            color: var(--dark-text);
            font-weight: 600;
            margin-bottom: 0.75rem;
            font-size: 1.1rem;
        }
        
        .product-card .card-text {
            color: var(--light-text);
            font-size: 0.9rem;
            line-height: 1.5;
            flex-grow: 1;
            margin-bottom: 1rem;
        }
        
        .product-card .price-tag {
            color: var(--primary-color);
            font-weight: 700;
            font-size: 1.2rem;
            margin-top: auto;
        }
        
        .category-badge {
            background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
            color: white;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 500;
            display: inline-block;
            margin-bottom: 1rem;
        }
        
        .btn-view-details {
            background: var(--primary-color);
            border: none;
            padding: 0.6rem 1.5rem;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s ease;
            margin-top: auto;
            width: 100%;
        }
        
        .btn-view-details:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
        }
        
        .search-box {
            position: relative;
        }
        
        .search-box .form-control {
            padding-left: 2.5rem;
            border-radius: 50px;
            border: 1px solid #e0e0e0;
            height: 50px;
            font-size: 1rem;
        }
        
        .search-box .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--light-text);
            z-index: 10;
        }
        
        .btn-search {
            background: var(--primary-color);
            border: none;
            color: white;
            border-radius: 50px;
            padding: 0.5rem 1.8rem;
            height: 50px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-search:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
        }
        
        .filter-select {
            border-radius: 50px;
            border: 1px solid #e0e0e0;
            height: 50px;
            padding-left: 1rem;
            font-size: 1rem;
        }
        
        .filter-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.15);
        }
        
        .no-products {
            padding: 4rem 2rem;
            text-align: center;
            background: white;
            border-radius: 16px;
            box-shadow: var(--card-shadow);
        }
        
        .no-products i {
            font-size: 3.5rem;
            color: #ddd;
            margin-bottom: 1.5rem;
        }
        
        .product-count {
            color: var(--primary-color);
            font-weight: 600;
            margin-left: 0.5rem;
        }
        
        @media (max-width: 768px) {
            body {
                padding-top: 70px;
            }
            
            .page-header {
                padding: 2rem 0;
                margin-bottom: 2rem;
            }
            
            .search-box, .filter-select-container {
                margin-bottom: 1rem;
            }
        }
        
        .footer {
            background: var(--dark-text);
            color: white;
            padding: 3rem 0;
            margin-top: 5rem;
        }
    </style>
</head>
<body>
    <%- include('../partials/customer-nav') %>
    
    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <h1>Discover Amazing Products</h1>
            <p class="lead">Browse our curated collection of high-quality products</p>
            <p class="mb-0">Showing <span class="product-count"><%= products.length %></span> products</p>
        </div>
    </div>
    
    <div class="container">
        <%- include('../partials/flash') %>
        
        <!-- Search and Filter -->
        <div class="row mb-5">
            <div class="col-lg-8 mb-3">
                <form action="/" method="GET" class="d-flex">
                    <div class="search-box flex-grow-1 me-3">
                        <i class="fas fa-search search-icon"></i>
                        <input type="text" class="form-control" name="search" placeholder="Search products by name, description..." value="<%= searchQuery %>">
                    </div>
                    <button type="submit" class="btn btn-search">
                        <i class="fas fa-search me-2"></i> Search
                    </button>
                </form>
            </div>
            <div class="col-lg-4">
                <form action="/" method="GET">
                    <div class="filter-select-container">
                        <select class="form-select filter-select" name="category" onchange="this.form.submit()">
                            <option value="">All Categories</option>
                            <% categories.forEach(category => { %>
                                <option value="<%= category._id %>" <%= selectedCategory == category._id ? 'selected' : '' %>>
                                    <%= category.name %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                </form>
            </div>
        </div>
        
        <!-- Products Grid -->
        <% if (products.length > 0) { %>
            <div class="row g-4">
                <% products.forEach(product => { %>
                    <div class="col-xl-3 col-lg-4 col-md-6">
                        <div class="card product-card">
                            <div class="product-img-container">
                                <img src="/uploads/<%= product.image %>" class="product-img" alt="<%= product.name %>">
                            </div>
                            <div class="card-body">
                                <div class="category-badge">
                                    <%= product.category.name %>
                                </div>
                                <h5 class="card-title"><%= product.name %></h5>
                                <p class="card-text"><%= product.description.substring(0, 100) %>...</p>
                                <% if (product.price) { %>
                                    <div class="price-tag mb-3">$<%= product.price.toFixed(2) %></div>
                                <% } %>
                                <a href="/product/<%= product.slug %>" class="btn btn-view-details">
                                    <i class="fas fa-eye me-2"></i> View Details
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-products">
                <i class="fas fa-search"></i>
                <h3 class="mb-3">No Products Found</h3>
                <p class="text-muted mb-4">Try adjusting your search or filter to find what you're looking for.</p>
                <a href="/" class="btn btn-primary px-4 py-2">
                    <i class="fas fa-sync-alt me-2"></i> Clear Filters
                </a>
            </div>
        <% } %>
    </div>
    
    <!-- Optional Footer -->
    <div class="footer mt-5">
        <div class="container text-center">
            <p>&copy; 2025 Product Portal. All rights reserved pabitra barik.</p>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Add smooth scrolling to the page
        document.addEventListener('DOMContentLoaded', function() {
            // Add animation to cards on load
            const cards = document.querySelectorAll('.product-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                
                setTimeout(() => {
                    card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, 100 + (index * 100));
            });
            
            // Update product count animation
            const productCount = document.querySelector('.product-count');
            if (productCount) {
                const count = parseInt(productCount.textContent);
                productCount.textContent = '0';
                
                let current = 0;
                const increment = count / 50;
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= count) {
                        current = count;
                        clearInterval(timer);
                    }
                    productCount.textContent = Math.floor(current);
                }, 30);
            }
        });
    </script>
</body>
</html>